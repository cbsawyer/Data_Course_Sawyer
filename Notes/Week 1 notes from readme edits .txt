Week 1 Notes:

To rename a file, the mv command followed by oldfilename then newfilename
? How to easily merge and repository files?
So echo is write, add is enter, commit is save, push is to send it and sync it to the repository, pull is to pull changes from the repository to here. 
nano [file name] to edit a text file
-m means "message"
? What are the branches for in github?